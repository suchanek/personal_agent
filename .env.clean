# Personal AI Agent Environment Variables
# Clean and organized version

# =============================================================================
# BASIC CONFIGURATION
# =============================================================================
LOG_LEVEL=INFO
USER_ID=Eric
SHOW_SPLASH_SCREEN=False

# =============================================================================
# DIRECTORY CONFIGURATION
# =============================================================================
ROOT_DIR=/
HOME_DIR=/Users/egs
DATA_DIR=/Users/Shared/personal_agent_data
REPO_DIR=/Users/egs/repos

# =============================================================================
# SERVER URLS AND PORTS
# =============================================================================
WEAVIATE_URL=http://localhost:8080
OLLAMA_URL=http://localhost:11434
REMOTE_OLLAMA_URL=http://tesla.local:11434

# LightRAG Service URLs
LIGHTRAG_URL=http://localhost:9621
LIGHTRAG_MEMORY_URL=http://localhost:9622

# Port configurations
PORT=9621
LIGHTRAG_PORT=9621
LIGHTRAG_MEMORY_PORT=9622

# =============================================================================
# FEATURE FLAGS
# =============================================================================
USE_WEAVIATE=False
USE_MCP=True

# =============================================================================
# AI MODEL CONFIGURATION
# =============================================================================
LLM_MODEL=qwen3:1.7b
EMBEDDING_MODEL=nomic-embed-text

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================
STORAGE_BACKEND=agno
AGNO_STORAGE_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}
AGNO_KNOWLEDGE_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}/knowledge

# LightRAG Storage Directories
LIGHTRAG_STORAGE_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}/rag_storage
LIGHTRAG_INPUTS_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}/inputs
LIGHTRAG_MEMORY_STORAGE_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}/memory_rag_storage
LIGHTRAG_MEMORY_INPUTS_DIR=${DATA_DIR}/${STORAGE_BACKEND}/${USER_ID}/memory_inputs

# =============================================================================
# TIMEOUT CONFIGURATION (only override defaults if needed)
# =============================================================================
# All timeout variables have sensible defaults in settings.py:
# HTTPX_TIMEOUT=7200, HTTPX_CONNECT_TIMEOUT=600, HTTPX_READ_TIMEOUT=7200
# HTTPX_WRITE_TIMEOUT=600, HTTPX_POOL_TIMEOUT=600, OLLAMA_TIMEOUT=7200
# OLLAMA_KEEP_ALIVE=3600, OLLAMA_NUM_PREDICT=16384, OLLAMA_TEMPERATURE=0.1
# LLM_TIMEOUT=7200, EMBEDDING_TIMEOUT=3600, PDF_CHUNK_SIZE=1024

# Uncomment and modify only if you need different values:
# HTTPX_TIMEOUT=7200
# OLLAMA_TIMEOUT=7200
# LLM_TIMEOUT=7200
