# Personal Agent Dashboard - Environment Configuration Example
# Copy this file to .env and customize the values for your setup

# =============================================================================
# EXTERNAL SERVICE URLS
# =============================================================================

# LightRAG Services
LIGHTRAG_URL=http://host.docker.internal:9621
LIGHTRAG_MEMORY_URL=http://host.docker.internal:9622

# Ollama Service
OLLAMA_URL=http://host.docker.internal:11434

# For Linux users, replace 'host.docker.internal' with your actual IP or use:
# LIGHTRAG_URL=http://172.17.0.1:9621
# LIGHTRAG_MEMORY_URL=http://172.17.0.1:9622
# OLLAMA_URL=http://172.17.0.1:11434

# =============================================================================
# DASHBOARD CONFIGURATION
# =============================================================================

# User Configuration
USER_ID=dashboard_user

# Logging
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Theme (optional)
# THEME_MODE=dark

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Docker socket path (usually doesn't need to be changed)
DOCKER_HOST=unix:///var/run/docker.sock

# =============================================================================
# OPTIONAL: ADVANCED CONFIGURATION
# =============================================================================

# Python path (usually set automatically)
# PYTHONPATH=/app/src

# Disable Streamlit usage stats
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. For macOS and Windows Docker Desktop users:
#    Use 'host.docker.internal' to access services running on the host
#
# 2. For Linux users:
#    Use the Docker bridge IP (usually 172.17.0.1) or your actual host IP
#
# 3. If running services in other containers:
#    Use the container names or service names from docker-compose
#
# 4. To find your Docker bridge IP on Linux:
#    docker network inspect bridge | grep Gateway
